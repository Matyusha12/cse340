<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% orders.forEach(order => { %>
            <li>Order ID: <%= order.order_id %>, Amount: <%= order.order_amount %>, Date: <%= order.order_date %></li>
        <% }); %>
    </ul>
</body>